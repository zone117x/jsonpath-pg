{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "command": "cmake ../src/c && make clean && make",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}/build"
      }
    }
  ]
}